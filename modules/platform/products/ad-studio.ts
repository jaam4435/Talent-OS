import type { ProductDefinition } from '@/modules/platform/contracts/product-registry'

export const AD_STUDIO_PRODUCT: ProductDefinition = {
  productId: 'ad_studio',
  displayName: 'AI Ad Studio',
  enabled: false,
  defaultConfig: {},
  metadata: {
    description: 'AI advertising studio — registered, not yet enabled',
  },
}
